name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        env:
          GITHUB_USERNAME: ${{ secrets.GH_USERNAME }}
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
            # Quit existing screen session if any
            screen -S funddrama -X quit || true

            # Start a new screen session with logging enabled
            screen -dmL -S funddrama

            # Change to the project directory within the screen session
            screen -S funddrama -p 0 -X stuff "cd fund-the-drama && printf \\r"
            sleep 5

            # Pull latest changes
            screen -S funddrama -p 0 -X stuff "git pull https://${GITHUB_USERNAME}:${GITHUB_TOKEN}@github.com/tensorlabs-io/fund-the-drama.git && printf \\r"
            sleep 30

            # Install dependencies
            screen -S funddrama -p 0 -X stuff "npm install && printf \\r"
            sleep 300

            # Build the project
            screen -S funddrama -p 0 -X stuff "npm run build && printf \\r"
            sleep 300

            # Start the project
            screen -S funddrama -p 0 -X stuff "npm run start && printf \\r"
            sleep 30

            # Print the screen session logs for debugging
            screen -S funddrama -X hardcopy /tmp/screenlog.txt
            cat /tmp/screenlog.txt
          EOF
